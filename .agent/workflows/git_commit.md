---
description: Git 커밋 워크플로우
---

# Git 커밋 워크플로우

## 단계

// turbo-all
1. 변경사항 확인: `git status`
2. 변경 파일 추가: `git add <files>`
3. 커밋: `git commit -m "feat: [작업 내용]"`
4. 푸시 (선택): `git push origin main`

## 커밋 메시지 규칙
- `feat:` 새 기능/문서 추가
- `update:` 기존 문서 수정
- `fix:` 오류 수정
- `docs:` 문서 정리
